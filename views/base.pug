doctype html
html
    head
        block head
            meta(charset="UTF-8")
            meta(name="viewport" content="width=device-width, initial-scale=1.0")
            link(href="https://fonts.googleapis.com/css?family=Lato:299,300i,700" rel="stylesheet")
            link(rel="stylesheet" href='/css/style.css')
            link(rel="shortcut icon" type="image/png" href='/img/favicon.png')
            title Natours | #{title} 

    body
        // HEADER 
        include _header 
        
        // CONTENT
        block content
            h3 This is placeholder

        // FOOTER
        include _footer 
        script(src="https://js.stripe.com/v3/")
        script(src="/js/bundled/index.js")
        //- script(defer src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js")
        //- script(defer src="/js/mapbox.js")
        //- script(defer src="/js/login.js")
